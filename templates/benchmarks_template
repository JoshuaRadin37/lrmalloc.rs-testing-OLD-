BENCHMARKS = {} 
TARGET = all
all:
	for benchmark in $(BENCHMARKS); do \
		cd $$benchmark && make && cd .. ; \
	done;

clean:
	for benchmark in $(BENCHMARKS); do \
		cd $$benchmark && make clean && rm Makefile && cd .. ; \
	done;
